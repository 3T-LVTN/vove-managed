server {
  listen 80;
  server_name frontend;
  location / {
    root /usr/share/nginx/html;
    try_files $uri /index.html;
  }
  location /api/prediction/ {
    access_log /var/log/nginx/vove_be.log;
    error_log /var/log/nginx/vove_be.error.log;
    proxy_pass http://localhost:5001/api/prediction/;
  }

  location /api/ {
    access_log /var/log/nginx/vove_be.log;
    error_log /var/log/nginx/vove_be.error.log;
    proxy_pass http://localhost:5001/api/;
  }
}
