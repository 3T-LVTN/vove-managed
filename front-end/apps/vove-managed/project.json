{
  "name": "vove-managed",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/vove-managed/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/vove-managed"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nrwl/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "vove-managed:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "vove-managed:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "vove-managed:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nrwl/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "vove-managed:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "vove-managed:build:development"
        },
        "production": {
          "buildTarget": "vove-managed:build:production"
        }
      }
    },
    "test": {
      "executor": "@nrwl/vite:test",
      "outputs": ["coverage/apps/vove-managed"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/apps/vove-managed"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/vove-managed/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
