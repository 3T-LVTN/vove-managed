import { userManagementFrameworksMongoose } from './user-management-frameworks-mongoose';

describe('userManagementFrameworksMongoose', () => {
  it('should work', () => {
    expect(userManagementFrameworksMongoose()).toEqual(
      'user-management-frameworks-mongoose'
    );
  });
});
